/payments/calculate:
  post:
    summary: Calculate monthly payments without lender information.
    description: |
     Wiki Page: http://git.truecarcorp.com/jkreutzbender/swagger_documentation_example/wiki/PaymentsCalculate
    parameters:
      - in: body
        name: params
        description: The payload for the calculation
        required: true
        schema:
          $ref: '#/definitions/PaymentCalculationPayload'
    tags:
      - Payments
    responses:
      '200':
        description: Successful request
        schema:
          type: array
          items:
            $ref: "#/definitions/PaymentsResponse"
      '400':
        $ref: "#/definitions/BadRequestResponse"