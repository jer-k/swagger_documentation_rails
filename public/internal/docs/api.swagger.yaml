swagger: '2.0'
info:
  title: Financing API
  description: Providing Finance and Lease information to TrueCar customers and dealers
  version: 1.0.0
basePath: /
produces:
  - application/json
paths:
  $ref_payments_calculate: paths/payments/calculate.yaml

definitions:
  BadRequestResponse:
    description: Required parameters are missing or invalid.
    properties:
      message:
        type: string
        description: Error message

  RequestTimeoutResponse:
    description: The request timed out.

  $ref_payments_calculate: definitions/payments/calculate.yaml
  $ref_payments_response: definitions/payments/response.yaml